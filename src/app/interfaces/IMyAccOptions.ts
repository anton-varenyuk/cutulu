export interface IMyAccOptions {
  userInfoEditable?: boolean;
  userPasswordEditable?: boolean;
  passwordIsUpdatedRecently?: boolean;
  userEmailEditable?: boolean;
  emailUpdatedRecently?: boolean;
}
